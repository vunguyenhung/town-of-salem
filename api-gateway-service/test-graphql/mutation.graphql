mutation sendCommand($command: CommandInput!){
    sendCommand(command: $command)
}

mutation register($user: UserInput!){
    register(user: $user)
}

mutation lobby($token: String!){
    joinLobby(token: $token)
}

mutation lobby($token: String!){
    leaveLobby(token: $token)
}